<script lang="ts">
  import { defineComponent, PropType } from '@nuxtjs/composition-api';

  /**
   * AdvisoryServiceAvailableLanguages.vue
   *
   * a small component for rendering the available languages of the advisory service
   */
  export default defineComponent({
    name: 'AdvisoryServiceAvailableLanguages',
    props: {
      /**
       * the availableLanguages array.
       */
      availableLanguages: {
        type: Array as PropType<string[]>,
        required: true,
      },
    },
	    setup() {
      return {};
    },
  });
</script>